{
  "name": "@rentora/types",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev:web": "pnpm --filter @rentora/web dev",
    "dev:api": "pnpm --filter @rentora/api start:dev",
    "dev": "pnpm --parallel --filter @rentora/web --filter @rentora/api dev",
    "build": "pnpm --recursive build",
    "lint": "pnpm --recursive lint"
  }
}
