{
  "name": "rentora-root",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev:web": "pnpm --filter @rentora/web dev",
    "dev:ssr": "pnpm --filter @rentora/ssr start:dev",
    "dev": "pnpm --parallel --filter @rentora/web --filter @rentora/ssr dev",
    "build": "pnpm --recursive build",
    "lint": "pnpm --recursive lint"
  }
}
